Install Git for Windows: This includes Git Bash.
Install the Azure CLI on Windows: Download and run the Microsoft Installer (MSI) for the Azure CLI. This installer typically adds the az command to your Windows system's PATH environment variable automatically. Verify the installation: After installation, close and reopen your Git Bash terminal, then run:az version
az vm create --resource-group RG1 --location eastus
